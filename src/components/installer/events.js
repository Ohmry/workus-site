import Vue from 'vue'

const AppAlertEvents = new Vue({
  name: 'AppAlert'
})

const AppConfirmEvents = new Vue({
  name: 'AppConfirm'
})

const AppContextMenuEvents = new Vue({
  name: 'AppContextMenu'
})

export { AppAlertEvents, AppConfirmEvents, AppContextMenuEvents }
